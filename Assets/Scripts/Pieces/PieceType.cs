public enum PieceType
{
    None = 0,
    Entry = 1,
    Target = 2,
    Block = 3,

    // Old Reflect behavior should become Mirror
    Mirror = 4,

    // New one-sided reflector
    Reflect = 5,

    Checkpoint = 6,
    Portal = 7
}